# 多功能助手 Agent 开发进度

本文档用于记录多功能助手 Agent 从早期工程化方案到当前主链路完成后的开发进度。后续新对话可以先阅读本文档、`agent整体设计.md`、`开发方案.md`，再进入对应层的专项进度文档继续开发。

## 0. 最新状态（重点必看）

截至 2026-08-18，Analyzer V1、Planner V1、ReActExecutor V1 第二阶段、Models V1、Tools V1 均已完成，Agent 核心主链路已经从早期原型收敛为 Planner-guided ReAct 执行闭环。

最新进度以各层进度文档为准：

```text
src/agent/Analyzer层开发步骤与进度.md
src/agent/Analyzer层设计决策汇总.md
src/agent/Planner层开发步骤与进度.md
src/agent/Planner层设计决策汇总.md
src/agent/ReActExecutor层开发步骤与进度.md
src/agent/ReActExecutor层设计决策汇总.md
src/agent/ReActExecutor第二阶段开发步骤与进度.md
```

当前已完成：

- Analyzer V1：任务理解、模式识别、意图和参数提取、风险策略、复杂度评分、工具策略、日志和回归测试已完成。
- Planner V1：结构化 `TaskPlan / TaskUnit / PlanStep`、多步骤计划、策略分支、依赖关系、规则模板、LLM planner fallback、日志和回归测试已完成。
- ReActExecutor V1：模型驱动的 Planner-guided ReAct 主循环、`ActionPacket` 协议、Observation、Checker、retry、fallback、确认暂停与恢复、事件流、结构化结果、安全不变量和最终验收已完成。
- Models V1：统一模型协议、provider 抽象、结构化输出、重试与观察性封装、mock / 真实 provider 接入和回归测试已完成。
- Tools V1：统一 `ToolResult / ToolSpec / ToolCallRequest / ToolPolicy / ToolRuntime`、文件与命令工具、web_search、MCP V1、工具日志和跨层验收已完成。
- 旧顺序 Executor 已退出正式主链路，默认执行路径只使用 ReActExecutor；旧 Executor 仅作为早期原型/历史诊断参考，`legacy` 只表示显式兼容/迁移开关，不作为自动回退。

当前验证命令：

```text
python -m pytest tests -q
```

结果：

```text
745 passed, 4 skipped
```

下一步建议：

```text
继续开发 Models 层 V1 正式化。
```

原因：

- Analyzer、Planner、ReActExecutor 的 V1 主链路已经完成。
- ReActExecutor 已经依赖真实模型稳定生成 `ActionPacket`。
- 下一步应优先补齐模型配置中心、结构化输出协议、超时、重试、错误封装和真实 provider 集成测试。

## 1. 项目整体目标

当前项目目标是把一个本地原型级多功能助手 Agent，逐步升级成可工程化落地的任务型智能助手系统。

目标使用场景包括：

- 日常个人效率助手。
- Word、Excel、PDF、Markdown、TXT、CSV、JSON 等文件读取、理解、总结和写入。
- 文件目录和资源管理。
- 项目设计、开发、测试、调试、部署等软件工程全流程。
- 数据分析、报告生成、内容整理。
- 中英文混合输入的通用多功能任务处理。

目标 Agent 主链路已经确定为：

```text
  -> Analyzer
  -> Planner
      生成 TaskPlan / TaskUnit / PlanStep
  -> ReAct Executor
      Reasoning -> Decision -> Tool -> Observation -> Checker
  -> Response
```

其中 Analyzer 是下一阶段最核心的开发模块，负责理解任务、识别意图、判断风险、计算复杂度和给 Planner/Executor 提供结构化决策输入。

## 2. 已完成的核心文档

### 2.1 `开发方案.md`

已完成一份完整的工程化开发方案，主要内容包括：

- 当前项目代码结构盘点。
- 已有能力和问题分析。
- 多功能助手 Agent 的目标产品形态。
- 推荐总体架构。
- Agent、模型、工具、记忆、RAG、安全层的模块设计。
- 分阶段开发路线图。
- 测试方案、部署方案、风险清单和 V1 验收标准。

该文档的定位是项目总蓝图，后续所有模块开发都应参考它，但具体编码时应以当前阶段的专项设计文档为准。

### 2.2 `src/agent/Analyzer层设计决策汇总.md`

已根据多轮问答整理完成 Analyzer 层专项设计决策。该文档是下一轮 Analyzer V1 开发的直接需求基线。

它已经明确：

- Analyzer 的职责边界。
- `solo/chat` 模式设计。
- 内置核心意图集合。
- 多意图识别规则。
- 规则、分类器、LLM 兜底的识别顺序。
- 参数、实体、文件路径、文件类型、编辑模式识别要求。
- 文件操作风险策略。
- 工具能力判断要求。
- 七维复杂度评分模型。
- 工程任务阶段和技术栈识别。
- `task_type` 输出。
- 日志、配置、pending intent 存储方式。
- Analyzer V1 输出字段清单。
- 下一轮开发顺序。

### 2.3 Analyzer 问答原始文件

以下文件记录了 Analyzer 设计问答过程，是 `Analyzer层设计决策汇总.md` 的来源：

```text
src/agent/回答Analyzer层的问题.txt
src/agent/继续补充Analyzer层问题.txt
src/agent/继续回答Analyzer问题2.0.txt
src/agent/继续回答Analyzer问题3.0.txt
```

这些问答已经汇总完成，后续开发优先看 `Analyzer层设计决策汇总.md`，只有需要追溯具体回答背景时再看这些原始 txt。

## 3. 已完成的基础层代码修复

当前代码已经完成一轮基础工程化修复，目的是让后续继续开发 Analyzer、Planner、Executor 和工具层时有一个更稳定的底座。

### 3.1 入口层

当前 `main.py` 已经从早期不稳定导入方式调整为标准 `src.*` 导入。

已完成：

- 使用 `from src.xxx import ...`。
- 增加 `build_agent()` 作为装配入口。
- CLI 主循环保留简单交互能力。
- 支持 `quit`、`exit`、`退出` 退出。
- 默认加载 `.env`，缺少 `python-dotenv` 时也能降级运行。

当前入口状态：

- 可以作为本地 CLI 原型入口继续使用。
- 还不是最终服务化入口。
- 后续仍需要 API 层、SessionManager、日志上下文和更完整的启动健康检查。

### 3.2 包结构

已完成：

- `src/__init__.py`
- `src/agent/__init__.py`
- `src/core/__init__.py`
- `src/memory/__init__.py`
- `src/models/__init__.py`
- `src/rag/__init__.py`
- `src/tools/__init__.py`

这一步让 `src` 下模块可以按包结构导入，避免继续依赖临时 `sys.path.append`。

### 3.3 配置层

已新增 `src/core/config.py`。

当前支持的配置包括：

- `AGENT_WORKSPACE_ROOT`
- `MODEL_NAME`
- `DEFAULT_CHAT_MODEL`
- `VECTOR_STORE_PATH`
- `ENABLE_CODE_EXECUTION`
- `ENABLE_FILE_WRITE`

已完成的配置设计：

- 使用 `Settings` dataclass 统一承载配置。
- 使用 `get_settings()` 缓存配置读取。
- 代码执行默认关闭。
- 文件写入默认开启，但受工作区限制。

当前缺口：

- Analyzer 专属配置尚未实现。
- 还没有 `config/analyzer/` 目录。
- 还没有日志级别、Agent 模式、风险规则、复杂度权重等配置文件。

### 3.4 `.env.example`

已更新 `.env.example`，当前包含：

```env
MODEL_NAME=mock
DEFAULT_CHAT_MODEL=mock
AGENT_WORKSPACE_ROOT=.
VECTOR_STORE_PATH=./storage/vector_store.pkl
ENABLE_CODE_EXECUTION=false
ENABLE_FILE_WRITE=true

OPENAI_API_KEY=
QIANWEN_API_KEY=
DOUBAO_API_KEY=
DASHSCOPE_API_KEY=
BING_API_KEY=
```

这保证项目在没有真实模型 API key 的情况下也能通过 mock 模型运行。

### 3.5 模型层

当前模型层已经收敛为：

```text
src/models/base_model.py
src/models/mock_model.py
src/models/model_manager.py
src/models/openai_model.py
src/models/qianwen_model.py
src/models/doubao_model.py
```

已完成：

- `ModelManager` 默认使用 `MockModel`。
- 支持按 `MODEL_NAME` 选择 `mock/openai/qianwen/doubao`。
- `ModelManager.generate()` 做了异常兜底。
- `ModelManager.stream_generate()` 做了异常兜底。
- 增加 `health_check()`。
- 增加 `get_model_info()`。

当前状态：

- 已具备多模型适配的基本形态。
- 还不是完整模型路由层。
- 后续仍需要 token 统计、超时、重试、熔断、调用日志和 embedding model 分离。

### 3.6 工具层

已新增或稳定了统一工具返回结构 `src/tools/base.py` 中的 `ToolResult`。

`ToolResult` 当前字段：

```text
success
data
message
error
code
```

已完成：

- `ToolResult.ok()`
- `ToolResult.fail()`
- `ToolResult.to_text()`
- `ToolResult.to_dict()`

`ToolManager` 当前统一通过 `run_tool()` 返回 `ToolResult`。

当前注册工具包括：

- `document_parser`
- `text_processor`
- `math_calculator`
- `translator`
- `time_query`
- `search_tool`
- `code_executor`
- `file_writer`

已经修复或约束的高风险点：

- `CodeExecutor` 默认关闭，需要 `ENABLE_CODE_EXECUTION=true` 才能执行。
- `CodeExecutor` 执行目录限制在 `workspace_root`。
- `CodeExecutor` 超时时间最多 30 秒。
- `FileWriter` 限制只能写入工作区内路径。
- `FileWriter` 不允许默认覆盖已有文件，除非传入 `overwrite=True`。
- `SearchTool` 在缺少 `BING_API_KEY` 时返回失败，不再假装可用。

当前缺口：

- 工具还没有统一参数 schema。
- 工具还没有统一风险等级字段。
- 工具注册仍是硬编码字典，不是正式 ToolRegistry。
- `delete_file`、`move_file`、`copy_file`、`rename_file` 等文件管理工具尚未正式实现。
- 工具能力还没有和 Analyzer 的 `tool_mapping.json` 打通。

### 3.7 Agent 编排层

当前 `src/agent/react_agent.py` 已经改成更清晰的主链路：

```text
add user message
  -> complexity_analyzer.analyze()
  -> planner.create_plan()
  -> executor.execute()
  -> add assistant message
  -> return response
```

已完成：

- `ReactAgent` 不再直接把复杂逻辑堆在一个方法里。
- 引入 `Planner`。
- 引入 `Executor`。
- 主流程已经对齐 `Analyzer -> Planner -> Executor` 的方向。

当前缺口：

- `ReactAgent` 仍是轻量编排层，不是最终 Orchestrator。
- 没有 SessionManager。
- 没有 trace_id、run_id、step 状态追踪。
- 没有中途确认、暂停、恢复机制。
- 没有正式 Responder。

### 3.8 Planner

已新增 `src/agent/planner.py`。

当前数据结构：

```text
PlanStep
TaskPlan
```

已完成：

- 根据 `task.execution_strategy` 生成计划。
- `micro` 任务生成单步工具计划。
- `macro` 任务生成澄清计划。
- `meso/meso_advanced` 任务生成基础分析和回答步骤。
- `calculate` 可以路由到 `math_calculator`。
- `read` 带文件参数时可以路由到 `document_parser`。
- `translate` 可以路由到 `translator`。

当前缺口：

- Planner 仍是确定性骨架。
- 复杂任务还没有真正拆解多步。
- 没有工具候选排序。
- 没有确认动作计划。
- 没有结合 Analyzer 的完整结构化字段。

### 3.9 Executor

已新增 `src/agent/executor.py`。

当前数据结构：

```text
StepExecution
ExecutionResult
```

已完成：

- 可以执行结构化 `TaskPlan`。
- 有工具名时调用 `ToolManager.run_tool()`。
- 无工具名时调用模型生成回答。
- 任一步骤失败会提前返回失败结果。
- `macro` 澄清任务会返回固定澄清提示。

当前缺口：

- 没有确认机制。
- 没有重试策略。
- 没有步骤级日志。
- 没有并行执行。
- 没有文件 diff/patch 执行机制。
- 没有危险动作暂停和用户确认流程。

### 3.10 当前 ComplexityAnalyzer

当前 `src/agent/complexity_analyzer.py` 是 Analyzer 的基础雏形，不是最终 Analyzer V1。

当前已具备：

- `StructuredTask` dataclass。
- 文本清洗。
- 规则意图识别。
- 简单多意图连接词识别。
- 基础参数提取。
- 数字和算式提取。
- 文件路径提取。
- 基础实体提取。
- 模糊输入识别。
- 法律、医疗、金融、隐私风险关键词识别。
- 简单任务直通。
- 简单分数计算。
- `simple/medium/complex/ambiguous` 等级映射。
- `micro/meso/meso_advanced/macro` 执行策略映射。
- 接入 `IntentClassifier` 占位接口。
- 接入 `UncertaintyDetector` 占位接口。

当前不足：

- 还没有严格按 `复杂度判定.md` 的七维模型评分。
- 没有 `solo/chat` 模式识别。
- 没有 `task_type`。
- 没有完整内置意图集合。
- 没有多意图最多 4 个和阈值规则。
- 没有 missing parameters 和 clarification questions。
- 没有 `action_policy=allow|confirm|block`。
- 没有 `requires_confirmation` 和 `confirmation_reason`。
- 没有 `recommended_tools/available_tools/missing_tools/tool_strategy`。
- 没有 `file_info/file_type/edit_mode` 的正式字段。
- 没有技术栈和工程阶段识别。
- 没有 pending intents。
- 没有 analyzer.log。
- 没有 `raw_analysis_trace`。
- 没有 30-50 条 Analyzer 测试样例。

结论：

- 当前 `complexity_analyzer.py` 是下一轮 Analyzer V1 开发的主要改造入口。
- 下一轮不需要从零开始，但需要按 `Analyzer层设计决策汇总.md` 大幅扩展结构和逻辑。

### 3.11 分类器和不确定性检测

当前已存在：

```text
src/agent/intent_classifier.py
src/agent/uncertainty_detector.py
```

当前定位：

- 分类器未来会使用 Transformer 架构训练。
- 当前只需要保留统一接口。
- 分类器未训练完成时允许 Analyzer 走规则和 LLM 兜底。

下一轮注意：

- 不要把分类器训练作为 Analyzer V1 的阻塞项。
- 只需要让 Analyzer 能调用分类器接口，并在分类器未就绪时稳定降级。

### 3.12 记忆层和 RAG

当前已有：

```text
src/memory/short_term_memory.py
src/memory/long_term_memory.py
src/rag/rag_system.py
```

已完成：

- 短期记忆可以保存会话消息。
- 长期记忆保留了基础持久化和检索方向。
- RAGSystem 可以基于长期记忆做简单检索和回答包装。

当前定位：

- 这部分已经能支撑主流程占位运行。
- 还不是最终工程级记忆和 RAG。

后续缺口：

- SessionManager 尚未实现。
- 会话概要导出尚未实现。
- `storage/sessions/{session_id}/summary.md` 尚未落地。
- RAG 还缺 chunk、metadata、引用、重排和知识库导入管道。

## 4. Analyzer 层已经确认的关键设计

Analyzer 相关需求已经基本确认完毕，不需要下一轮继续大规模追问。

### 4.1 Analyzer 定位

Analyzer 是任务理解中枢，不执行工具，不直接完成任务。

它负责输出结构化分析结果，供 Planner 和 Executor 使用。

### 4.2 模式设计

支持两种模式：

- `solo`：Agent 默认全权负责完成用户任务。
- `chat`：Agent 只给指导、解释、步骤和答案，不主动执行。

全局模式通过配置控制：

```env
AGENT_MODE=solo
```

单轮输入可以临时覆盖模式，但不修改全局配置。

规则：

- 用户说“只告诉我步骤，不要执行” -> 当前轮 `chat`。
- 用户说“直接帮我完成” -> 当前轮 `solo`。
- 同一轮多个模式指令冲突时，以最后出现的明确指令为准。

### 4.3 内置核心意图

V1 内置意图包括：

```text
calculate
search
summarize
translate
write
analyze
plan
read_file
write_file
execute_code
extract
compare
recommend
chat
classify
convert_format
organize_files
list_files
find_files
rename_file
copy_file
move_file
delete_file
create_project
design_project
debug_code
run_test
deploy_project
generate_report
```

这些只是固定意图集合。用户出现其他合理意图时，Analyzer 仍需要提取，并进入 pending intent 流程。

### 4.4 新意图处理

新意图策略：

- LLM 可以生成新意图。
- 新意图不直接进入生产意图库。
- V1 写入 `storage/analyzer/pending_intents.json`。
- V1 不自动合并相似意图。
- 记录 `raw_name` 和 `normalized_name`。

进入 pending 的条件：

- LLM 提取出非内置意图。
- 置信度 >= 0.65。
- 不是明显闲聊。

### 4.5 文件操作和风险策略

默认允许：

- 创建文件。
- 写文件。
- 覆盖文件。
- 移动文件。
- 复制文件。
- 重命名文件。

必须确认：

- 删除文件。
- 执行代码。
- 执行命令。

必须拒绝：

- 删除系统目录。
- 执行明显危险命令。
- 访问工作区外敏感路径。

Analyzer 应输出：

```text
action_policy = allow | confirm | block
requires_confirmation
confirmation_reason
risk_level
risk_flags
```

Planner/Executor 负责真正暂停执行和提示用户确认。

### 4.6 文件格式支持

V1 一等支持：

```text
txt
md
pdf
docx
xlsx
csv
json
```

V1 暂不要求支持 `pptx`。

### 4.7 文件修改模式

Analyzer V1 只识别修改模式：

```text
edit_mode = full_overwrite | partial_edit
```

精确定位代码行、生成 patch、应用 patch 由后续 Planner/Executor 实现。

### 4.8 七维复杂度评分

V1 严格按 `src/agent/复杂度判定.md` 的七维模型实现，但权重和阈值配置化。

七维包括：

```text
uncertainty
steps
domain_risk
tools
information
data_processing
creativity
```

执行策略保持：

```text
micro
meso
meso_advanced
macro
```

### 4.9 技术栈和工程阶段识别

Analyzer 需要识别软件工程任务的阶段：

```text
design
develop
test
debug
deploy
document
```

技术栈 V1 用固定词表规则为主，LLM 兜底补充。

词表需要覆盖：

- Python 生态。
- Java 生态。
- C++ 生态。
- 前端框架。
- 后端框架。
- 数据库。
- 测试框架。
- 部署工具。
- 深度学习技术栈。

### 4.10 `task_type`

Analyzer 需要输出 `task_type`，建议枚举：

```text
qa
file_operation
document_understanding
software_engineering
data_analysis
content_generation
project_management
tool_operation
chat
```

### 4.11 日志和过程展示

Analyzer V1 需要保存日志：

```text
logs/analyzer.log
```

日志记录：

- 原始输入。
- 清洗后输入。
- 模式判断。
- 意图及分数。
- 多意图截断情况。
- 参数提取结果。
- 缺失参数。
- 澄清问题。
- 七维复杂度明细。
- 风险标记。
- action_policy。
- 推荐工具、可用工具、缺失工具。
- raw_analysis_trace。

普通用户 UI 不展示系统级字段，开发模式和日志可以展示完整细节。

## 5. 下一轮 Analyzer V1 需要新增的文件

建议新增配置目录：

```text
config/analyzer/
  analyzer_config.json
  intents.json
  intent_keywords.json
  risk_rules.json
  complexity_weights.json
  tech_stacks.json
  tool_mapping.json
```

建议新增运行数据文件：

```text
storage/analyzer/pending_intents.json
logs/analyzer.log
tests/fixtures/analyzer_cases.json
```

## 6. Analyzer V1 输出对象要求

Analyzer V1 输出结构至少包含：

```text
raw_input
cleaned_input
mode
mode_source
task_type
intents
intent_sequence
entities
parameters
missing_parameters
clarification_questions
file_info
edit_mode
project_stage
tech_stacks
risk_level
risk_flags
action_policy
requires_confirmation
confirmation_reason
dimension_scores
complexity_score
complexity_level
execution_strategy
recommended_tools
available_tools
missing_tools
tool_strategy
confidence_score
confidence_level
raw_analysis_trace
user_facing_summary
```

字段设计原则：

- 面向系统的字段必须结构化，方便 Planner、Executor、日志和测试使用。
- 面向用户的字段必须自然语言化，避免暴露系统内部枚举和分数。
- `raw_analysis_trace` 只用于开发调试和日志。
- Analyzer 只判断和路由，不执行工具。
- 删除文件、执行代码、危险命令必须交给 Planner/Executor 暂停确认或拒绝。

## 7. 历史记录：Analyzer V1 开发建议顺序

以下是早期进入 Analyzer V1 前的开发建议，当前这些内容已经完成，保留用于追溯 Analyzer 的开发来源：

1. 建立 `config/analyzer/` 配置目录。
2. 建立 Analyzer 配置加载器。
3. 重构 `src/agent/complexity_analyzer.py` 的数据结构，把 `StructuredTask` 扩展为完整 AnalysisResult。
4. 实现 `solo/chat` 模式识别和单轮覆盖。
5. 实现内置意图配置和关键词规则识别。
6. 实现多意图评分、阈值过滤、最多 4 个主要意图。
7. 接入 `IntentClassifier` 占位接口，并处理未训练完成时的降级。
8. 实现参数、实体、文件路径、文件类型、编辑模式识别。
9. 实现 `task_type`、工程阶段、技术栈识别。
10. 实现风险识别、确认策略、拒绝策略。
11. 实现七维复杂度评分。
12. 实现工具能力评估。
13. 实现 pending intents 记录。
14. 实现 `logs/analyzer.log`。
15. 增加 30-50 条 Analyzer 测试样例。
16. 调整 Planner/Executor 使用新的 Analyzer 输出结构。

## 8. 历史记录：Analyzer V1 阶段不建议优先做的事情

以下是早期 Analyzer V1 阶段的取舍建议。当前后续优先级以第 12 节为准：

- 不要先训练 Transformer 意图分类器。
- 不要先做 UI。
- 不要先做完整 FastAPI 服务化。
- 不要先做复杂 RAG。
- 不要先做多 Agent。
- 不要先实现精确 patch 引擎。
- 不要继续扩大量工具而不先完善 Analyzer 和工具协议。

当时的原因是任务理解层尚未工程化，Analyzer 不稳定会导致 Planner、Executor、工具选择和风险控制都无法稳定。当前 Analyzer V1 已完成。

## 9. 当前工作区状态提示

当前仓库存在一些未跟踪文档文件，主要是本轮新增或用户补充的 Analyzer 问答和汇总文档：

```text
src/agent/Analyzer层设计决策汇总.md
src/agent/回答Analyzer层的问题.txt
src/agent/继续补充Analyzer层问题.txt
src/agent/继续回答Analyzer问题2.0.txt
src/agent/继续回答Analyzer问题3.0.txt
```

这些文件不应删除。它们是下一阶段 Analyzer 开发的重要上下文。

仓库中还存在 `__pycache__` 文件，后续运行 Python 验证时建议使用：

```text
python -B ...
```

避免继续产生新的 pycache 变更。

## 10. 给下一个对话的直接指令建议

如果下一个新对话要继续开发，可以直接这样开始：

```text
请先阅读 开发进度.md 和 src/agent/Analyzer层设计决策汇总.md，然后按照文档开始实现 Analyzer V1。优先建立 config/analyzer 配置文件、重构 src/agent/complexity_analyzer.py 的输出结构，并补齐模式识别、意图识别、风险策略、七维复杂度评分、pending_intents、analyzer.log 和测试样例。
```

## 11. 当前结论

从 `开发方案.md` 到 `Analyzer层设计决策汇总.md` 之间，已经完成了三类工作：

1. 项目整体工程化方案已经明确。
2. 当前基础代码已经做过一轮底层修复，主链路收敛到 `Analyzer -> Planner -> Executor -> ToolResult`。
3. Analyzer 层的需求问答和设计决策已经整理完成，下一步可以直接进入代码开发。

以上是早期进入 Analyzer V1 前的历史结论。当前 Analyzer、Planner、ReActExecutor V1 均已完成，后续开发入口以第 12 节“最新进度回写”为准。

---

## 12. 最新进度回写（2026-08-05）

本节记录 Analyzer、Planner、ReActExecutor 完成后的最新状态。前面章节保留历史上下文；后续继续开发时应优先阅读本节和各层专项进度文档。

### 12.1 当前主链路

当前 Agent 主编排链路已经更新为：

```text
用户输入
  -> ReactAgent
  -> Analyzer
  -> Planner
  -> ReActExecutor
  -> 输出反馈处理器
  -> 用户反馈
```

其中：

```text
ReactAgent:
  轻量编排入口，串联 Analyzer、Planner 和当前默认 ReActExecutor。

Analyzer:
  理解用户输入，输出结构化 AnalysisResult。

Planner:
  消费 AnalysisResult，生成 TaskPlan / TaskUnit / PlanStep。

ReActExecutor:
  消费 TaskPlan，执行 Planner-guided ReAct 主循环。

输出反馈处理器:
  消费 ReActExecutor 产出的 ExecutionEvent / ExecutionResult，将执行过程和最终结果转换成用户可见反馈。
  当前主要由 ReactAgent.run / run_with_result / run_stream、EventStream 和 ExecutionResultBuilder 承担；后续 Runtime / API / Session 层会把它服务化。
```

ReActExecutor 的内部执行循环是：

```text
Thought
  -> Action(ActionPacket)
  -> 调用 Tool / Model / User / Control
  -> 根据真实调用结果生成 Observation
  -> Checker 判断继续、重试、fallback、等待用户、完成或失败
  -> 按过程产出 ExecutionEvent，并最终汇总 ExecutionResult
```

注意：

```text
Tool / Model 不是 ReActExecutor 之后的独立主链路层。
Observation / Checker 是 ReActExecutor 内部机制。
ExecutionResult / ExecutionEvent 是执行器和 ReactAgent 对上层返回的结构化输出协议，不是业务层。
输出反馈处理器只负责展示和交付，不负责重新规划、工具执行或安全决策。
执行器内部可以产生用户可见的步骤说明、计划摘要、工具进度和阶段性反馈；这些内容必须通过 ExecutionEvent 输出，并与开发日志、原始 prompt、内部调试信息分离。
Models 层是项目级基础模型服务层，Analyzer、Planner、ReActExecutor 以及后续 Memory / RAG 都可以调用它；旧顺序 Executor 不再作为正式适配目标。
Tools 层是项目级工具能力服务层，当前主要由 ReActExecutor 通过结构化 ActionPacket 调用；后续 Runtime/API 或其他受控入口也可以通过统一协议调用。
```

关键原则：

```text
本项目目标是模型作为大脑的灵活任务型 Agent，不是规则硬编码结果的固定流程助手。
```

后续开发和调试其他层时必须优先检查：

```text
需要理解、判断、生成、决策、总结、修复的地方，是否保留或调用了 Models 层。
规则是否只用于协议校验、安全边界、错误分类、timeout/retry/fallback 策略和测试兜底。
是否误把复杂计划、ActionPacket 决策、最终回答或工具结果总结写成固定规则生成。
```

典型模型调用位置：

```text
Analyzer:
  LLM fallback 做意图、参数、风险、复杂度和策略兜底。

Planner:
  LLM planner fallback 生成结构化 TaskPlan / TaskUnit / PlanStep。

ReActExecutor:
  模型生成 Thought / ActionPacket，模型 repair 非法 ActionPacket，call_model、fallback_to_model、复杂总结和 final answer 都走 Models 层。

Tools:
  web_search 的 model_builtin provider、后续模型翻译/摘要/改写工具通过 Models 层调用；普通工具只做真实执行。
```

当前主入口文件：

```text
main.py
src/agent/react_agent.py
```

当前核心专项文档：

```text
src/agent/Analyzer层开发步骤与进度.md
src/agent/Analyzer层设计决策汇总.md
src/agent/Planner层开发步骤与进度.md
src/agent/Planner层设计决策汇总.md
src/agent/ReActExecutor层开发步骤与进度.md
src/agent/ReActExecutor层设计决策汇总.md
src/agent/ReActExecutor第二阶段开发步骤与进度.md
```

### 12.2 已完成的开发

#### Analyzer V1

状态：已完成。

已完成内容：

- Analyzer 配置拆分和输出结构。
- 规则意图识别、分类器协议、LLM fallback 协议。
- 多意图识别、参数提取、文件信息识别、工程阶段和技术栈识别。
- 缺参澄清、pending intent、风险策略、确认策略、拒绝策略。
- 七维复杂度评分。
- 工具能力评估。
- Analyzer 日志和 fixture 回归。

边界：

- Analyzer 只负责理解、判断和路由。
- Analyzer 不直接执行工具，不直接替代 Planner 和 ReActExecutor。

#### Planner V1

状态：已完成。

已完成内容：

- 结构化 `TaskPlan / TaskUnit / PlanStep`。
- 消费 Analyzer 的 `intent_sequence`、`parameters`、`file_info`、`task_type`、`project_stage`、`tool_strategy`、`action_policy`。
- 支持澄清、确认、缺工具、拒绝、chat、model-only、普通可执行计划。
- 支持多步骤计划、依赖关系、规则模板、LLM planner fallback。
- Planner 日志和回归测试。

边界：

- Planner 不执行工具。
- Planner 负责产出结构化计划，不负责真实 Observation。

#### ReActExecutor V1

状态：第二阶段最终验收已完成。

已完成内容：

- 主循环入口契约与旧 skeleton 替换。
- ReAct Turn 状态结构。
- 模型决策 Prompt 上下文接入。
- `ActionPacket` 生成、校验与 repair。
- 单步和多步 ReAct 闭环。
- `TaskUnit / PlanStep` 顺序主循环。
- Checker 驱动转移。
- 自动 retry 和 fallback。
- `finish / fail / request_replan` 终止语义。
- chat / model-only 主路径。
- Observation 上下文压缩与 `input_from` 稳定性。
- 用户可见事件流增强。
- `execute_stream` / 事件订阅接口。
- 确认暂停与恢复闭环。
- command action 通过 Tool 层执行。
- Agent API 结构化结果暴露。
- 主循环日志。
- fake model sequence fixture 回归。
- 端到端测试从 skeleton 改为真实闭环。
- 旧 skeleton 边界清理与兼容测试调整。
- 完整安全回归与主循环不变量。
- 第二阶段文档回写和最终验收。

边界：

- ReActExecutor 不直接执行 shell。
- 命令必须走 Tool 层。
- 模型不能伪造 Observation。
- 用户可见 events 和开发 logs 分离。
- 旧顺序 Executor 已退出正式主链路，不作为正式 fallback；保留相关历史文件时也只用于诊断参考或后续清理，`legacy` 仅表示显式兼容切换。
- V1 版本不需要再开启 ReActExecutor 第三阶段。

最终验证：

```text
python -B -m unittest discover tests
```

结果：

```text
Ran 310 tests
OK
```

### 12.3 当前还未完成的层

下面这些层已有原型或基础文件，但还没有达到真实可用的工程成熟度。

#### P0：Models 模型服务层

当前状态：

- 已有 `BaseModel`、`MockModel`、`ModelManager`、OpenAI / Qianwen / Doubao provider 雏形。
- 默认 mock 可以支撑测试。
- 还不是稳定的真实模型服务层。

需要补齐：

- 模型配置中心。
- provider 参数统一。
- 模型路由。
- 结构化输出协议。
- JSON 解析和 repair。
- 超时、重试、熔断、限流。
- token、cost、latency、request_id 统计。
- 真实 provider 集成测试。

建议作为下一层优先开发。

#### P1：Tools 工具能力层

当前状态：

- 已有 `ToolResult`、`ToolManager`、`ToolRegistry`、基础工具和 command tool。
- 工具协议已经能被 ReActExecutor 消费。
- 真实工具能力还不够完整。

需要补齐：

- 完整 Tool schema。
- 文件管理工具。
- 文档解析增强。
- 搜索工具稳定化。
- CommandTool 安全矩阵。
- CodeExecutor 沙箱策略。
- 工具失败码和完整测试集。

#### P1：Runtime / API / Session 层

当前状态：

- `main.py` 是 CLI 原型。
- `ReactAgent` 已暴露 `run`、`run_with_result`、`run_stream`。
- 还没有正式 API 和 Session 生命周期。

需要补齐：

- 标准 CLI。
- FastAPI 或等价 API。
- 执行状态查询。
- 事件流订阅。
- 确认 / 恢复接口。
- SessionManager。
- 启动健康检查。

#### P2：Memory 层

当前状态：

- 已有短期记忆和长期记忆原型。

需要补齐：

- 会话隔离。
- token 压缩。
- 会话摘要导出与恢复。
- 长期记忆 metadata / source / score。
- Observation 历史持久化。

#### P2：RAG / 知识库层

当前状态：

- 已有 RAG 薄封装。

需要补齐：

- 文档导入管道。
- chunk。
- embedding model 分离。
- 向量库适配。
- source citation。
- 检索质量测试集。

#### P2：安全与权限层

当前状态：

- Analyzer、Planner、ReActExecutor 已经有风险策略和确认机制。
- 还没有独立全局权限中心。

需要补齐：

- 全局 SafetyPolicy 配置中心。
- workspace 读写权限。
- 命令权限策略。
- 网络访问策略。
- 审计日志。
- 日志脱敏。

#### P3：配置、日志、观测与验收工程

当前状态：

- 各层有局部配置、日志和测试。

需要补齐：

- 统一配置加载优先级。
- dev / test / prod 环境隔离。
- trace_id 全链路贯穿。
- 运行指标。
- 真实任务验收 fixture。
- CI 或一键回归脚本。

### 12.4 后续建议开发顺序

建议后续按以下路线继续：
（当前 ReactAgent 已作为轻量编排层串联 Analyzer、Planner 和 ReActExecutor，后续暂不单独开启编排层专项；若 Runtime / API / Session 开发中发现编排职责膨胀，再对 ReactAgent 做小范围收敛。）
```text
1. Models 层 V1 正式化（已完成）
2. Tools 层 V1 正式化（已完成）
3. Runtime / API / Session 层（优先顺序2）
4. Memory 层正式化+增强（优先顺序1）
5. RAG / 知识库层增强（优先顺序2）
6. 安全与权限层统一（安全策略层）（优先顺序2）
7. 配置、日志、观测与端到端验收工程（优先顺序3）
```
注意！注意！：有一个调整原则：这个顺序是主线，不是死板锁死。实际开发时如果发现某一层必须依赖后面的基础能力，可以小范围提前补。例如：

- 开发 Models 层时，如果需要统一配置，就可以先补一部分配置加载。
- 开发 Tools 层时，如果涉及命令或文件写入，就可以同步补一部分安全策略。
- 开发 Runtime/API 时，如果需要 `SessionManager`，就可以提前做 Memory 层里的会话管理最小闭环。

不过主推进方向不建议变。下一步应从 **Models 层 V1 正式化** 开始，因为 ReActExecutor 已经完成，真实运行最先会卡在模型结构化输出和 provider 稳定性上。
```

### 12.5 下一轮对话建议指令

如果下一轮继续开发，建议直接这样开始：

```text
请先阅读 agent整体设计.md、开发方案.md、开发进度.md，以及 src/models/Models层开发步骤与进度.md。
当前 Analyzer、Planner、ReActExecutor、Models、Tools V1 已完成。
如果继续开发，请先阅读对应后续层的进度文档，再从 Runtime / API / Session 层开始。
```

## 13. 实现最小可跑架构的后续开发顺序（临时版完整开发还是看上面的12.4）

### 13.1 先回答当前架构关系

是的，当前这条主链路：

```text
Analyzer
-> Planner
   -> TaskPlan / TaskUnit / PlanStep
-> ReAct Executor
   -> Reasoning -> Decision -> Tool -> Observation -> Checker
-> Response
```

这几层已经可以统称为**Agent 核心编排层**。  
其中 `ReactAgent` 负责把这条链路组织起来，属于**编排的具体实现**；  
`Runtime` 则是把这套编排真正装配起来、启动起来、对外提供入口的**运行外壳 / 最后一环**。

简单说：

- **编排层**：负责内部推理与执行顺序。
- **Runtime**：负责把编排层接到 CLI / API / Session / 配置 / 日志 / 生命周期上。

所以 `Runtime` 可以理解为**编排落地后的系统入口层**，但它不是 Analyzer / Planner / Executor 这些内部层本身。

### 13.2 最小可跑架构后续必须完成的东西

如果目标是“先让 Agent 真的能跑起来，并且能写进简历”，后续最小闭环建议补齐这些：

1. **Runtime**
   - 负责装配 `Analyzer -> Planner -> Executor -> Memory -> Tools -> Models`。
   - 负责启动、停止、异常兜底、流式输出、会话注入。

2. **CLI 或 API**
   - 先有一个对外入口即可。
   - 如果先做 demo，优先 `CLI`。
   - 如果要给前端或服务端接入，再补 `API`。

3. **Session MVP**
   - `session_id`
   - 会话创建 / 恢复
   - 会话隔离
   - 当前轮次状态保存

4. **Memory 的最小可用部分**
   - 短期上下文保存
   - 多轮上下文拼接
   - 会话摘要保存 / 读取
   - 最近 N 轮消息保留

5. **上下文压缩**
   - 这是增强项，但对多轮会话很重要。
   - 当历史太长时，把更早内容压成 summary。
   - 保留最近消息窗口，避免上下文爆掉。

6. **最小验收工程**
   - 至少 2 到 3 个端到端 demo。
   - 至少 1 套健康检查。
   - 至少 1 套可重复运行的 smoke test。

### 13.3 推荐的实际开发顺序

```text
1. Memory / Context MVP 层

目标：
- 让 Agent 支持基于 session_id 的多轮对话。
- 让同一个 session 能读取前面的对话内容。
- 让会话结束后可以保存，并在后续恢复。
- 为 Runtime 提供统一的上下文读取接口。

当前范围：
- SessionManager
- session_id 管理
- ShortTermMemory
- 最近消息窗口
- ContextBuilder
- summary + recent_messages + current_input
- 会话保存与恢复

实现方向：
- 定义 Message、SessionState 等基础数据结构。
- SessionManager 负责创建、加载、保存和清理 session。
- ShortTermMemory 按 session 保存消息。
- ContextBuilder 统一生成传给 Agent 的上下文。
- 先按消息数量限制，不做复杂 token 压缩。

验收标准：
- 同一个 session 连续提问时，Agent 能读取前文。
- 不同 session 之间的历史互不混淆。
- 重启程序后可以恢复已有 session。
- ContextBuilder 能正确生成当前上下文。

暂不处理：
- 向量长期记忆。
- RAG。
- 自动摘要。
- 复杂 token 计算和历史压缩。


2. Runtime / CLI / API 入口层

目标：
- 把已有 Analyzer、Planner、ReActExecutor、Models、Tools 和 Memory 装配成可启动系统。
- 让用户可以通过 CLI 或 API 真实调用 Agent。
- 让一次请求完整走完“输入 -> 分析 -> 规划 -> 执行 -> 记忆更新 -> 输出”。

实现方向：
- 实现 build_runtime() 或统一启动装配函数。
- 注入 Model、Tools、Memory、ReactAgent、SessionManager。
- 优先完成 CLI，API 可以随后接入。
- 增加基础配置加载、错误处理和健康检查。

验收标准：
- 能通过一条命令启动 Agent。
- 能执行单轮对话。
- 能执行同一 session 的多轮对话。
- 能调用已有工具并返回结构化结果。
- 模型、工具或配置异常时能返回可读错误。


3. Context 管理增强层

目标：
- 让 Agent 在短、中、长三种对话场景下都能稳定使用上下文。
- 避免历史消息过长导致模型超出上下文窗口。
- 在压缩历史时尽量保留任务目标、关键事实和执行结果。

实现方向：
- 增加 token 统计或近似 token 计算。
- 设置上下文长度阈值。
- 保留 summary、最近消息和当前输入。
- 超过阈值时压缩较早的历史消息。
- 支持摘要失败时的降级策略，例如只保留最近消息。

验收标准：
- 对话较少时使用完整历史。
- 对话中等时使用 summary + 最近消息。
- 对话较多时能触发压缩且不超上下文窗口。
- 压缩后仍能理解当前任务和关键历史信息。


4. 最小可跑架构整体测试

目标：
- 验证整条 Agent 链路可以稳定运行。
- 验证 Agent 不只是能回复，而是能完成基础任务。

实现方向：
- CLI 对话 smoke test。
- Session 多轮对话测试。
- 会话保存和恢复测试。
- 工具调用测试。
- 文件读取、计算、搜索等基础任务测试。
- 任务失败、工具异常和模型异常测试。

验收标准：
- Agent 能正常启动。
- Agent 能正常进行多轮对话。
- Agent 能读取上下文并完成基础任务。
- Agent 能正确调用工具。
- Agent 失败时能返回明确状态。
- 测试可以重复运行，不依赖一次性脚本。


5. 长期记忆 / RAG / 进一步工程化

目标：
- 将最小可跑版本扩展为更完整的工程化 Agent 系统。

实现方向：
- 长期记忆。
- 文档导入和 RAG 检索。
- embedding 和向量库。
- 统一安全权限。
- 审计日志和可观测性。
- 部署、CI 和生产环境配置。

验收标准：
- 新能力不破坏现有 Runtime、Session 和多轮对话。
- 长期记忆和 RAG 可以独立测试。
- 关键运行链路具备日志、错误追踪和回归测试。
```

### 13.4 最简判断

如果只问“做到什么程度就算最小可跑”：

- **必须有**：`Runtime + 入口 + Session MVP + 基础 Memory`
- **最好有**：`多轮上下文 + 会话摘要`
- **后续再做**：`上下文压缩 + 长期记忆 + RAG`
