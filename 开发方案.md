# 多功能助手 Agent 工程化开发方案

## 1. 文档目标

本文档基于当前仓库现状，对多功能助手 Agent 的已有设计进行工程化梳理，给出一份可以直接落地执行的开发方案。方案覆盖以下内容：

- 已完成能力盘点
- 当前实现中的问题与改进建议
- 目标产品形态与技术架构
- 分阶段开发路线图
- 模块设计、接口规范、测试方案、部署方案
- 风险清单与验收标准

本文档既是后续开发蓝图，也可作为需求、研发、测试、部署协同的统一基线。

---

## 2. 当前项目现状评估

### 2.1 当前代码结构

当前仓库已初步形成以下模块：

- `main.py`：命令行入口
- `src/agent/`：Agent 调度、复杂度识别、意图识别相关逻辑
- `src/models/`：多模型管理与模型适配
- `src/memory/`：短期记忆、长期记忆
- `src/rag/`：RAG 检索封装
- `src/tools/`：文档解析、文本处理、翻译、搜索、代码执行、写文件等工具

整体方向是正确的，已经具备一个多功能 Agent 的基础骨架，说明你的设计思路已经覆盖了以下关键能力：

- 对话入口
- 模型路由基础能力
- 工具调用能力
- 任务复杂度判断意识
- 多步任务执行思路
- 短期/长期记忆设计
- RAG 知识检索方向

这部分属于“从 0 到 1 骨架搭建”阶段，已经完成了比较重要的启动工作。

### 2.2 已完成能力盘点

#### A. Agent 主流程

当前 `ReactAgent` 已具备以下设计意图：

- 接收用户输入
- 读取短期上下文
- 判断任务复杂度
- 区分简单任务与复杂任务
- 简单任务走单步执行
- 复杂任务尝试走任务规划、多步执行、结果汇总

这说明核心控制流已经初步建立。

#### B. 模型层

`ModelManager` 已体现多模型切换思路：

- 豆包模型
- 千问模型
- OpenAI 模型

这为后续做模型策略路由、成本控制、故障切换提供了基础。

#### C. 记忆层

- `ShortTermMemory` 已支持会话历史维护
- `LongTermMemory` 已支持向量化存储、检索、持久化

虽然实现仍偏原型，但方向具备工程演进价值。

#### D. 工具层

`ToolManager` 当前已接入多类工具：

- 文档解析
- 文本处理
- 数学计算
- 翻译
- 时间查询
- 搜索
- 代码执行
- 文件写入

这已经具备“多功能助手”的核心扩展点。

#### E. RAG 方向

`RAGSystem` 已封装长期记忆检索 + 上下文拼接 + 生成回答的流程雏形。

#### F. 复杂任务识别方向

目前仓库中存在两套复杂度分析思路：

- `src/agent/complexity_judge.py`
- `src/agent/complexity_analyzer_cankao.py`

这说明你已经在尝试把“单轮问答”升级为“任务型 Agent”，这是整个项目里很有价值的一步。

### 2.3 基于当前代码的复核结论

这次方案更新已明确参考了当前仓库里已经实现的主干代码，重点包括：

- `main.py`
- `src/agent/`
- `src/models/`
- `src/memory/`
- `src/rag/`
- `src/tools/`

基于这些代码，当前项目的真实状态可以更准确地描述为：

#### A. 入口层现状

当前入口是一个本地 CLI 原型，已经具备最基本的交互循环，但还存在明显问题：

- `main.py` 依赖 `sys.path.append` 做模块导入，说明包结构还未正规化
- 入口直接依赖不存在或未收敛的 `agent.complexity_analyzer`
- 退出分支和打印文案存在明显不完整/乱码迹象
- 初始化阶段没有任何容错、健康检查、依赖检查
- 没有会话 ID、日志上下文、错误兜底

结论：

- 当前入口适合本地原型验证
- 不适合作为工程主入口长期保留
- 后续必须切为“标准包入口 + CLI 入口 + API 入口”三层模式

#### B. Agent 编排层现状

当前 `react_agent.py` 已经实现了最重要的一层雏形：

- 收消息
- 读短期记忆
- 分简单/复杂任务
- 简单任务直接执行
- 复杂任务尝试规划、多步执行、结果汇总

但当前实现离工程化还有明显距离：

- 大量 TODO 尚未完成
- 规划结果仍是文本，不是结构化对象
- 步骤解析过于粗糙，基本是按行切分
- 工具执行结果没有统一标准返回
- 缺少失败重试、终止条件、动态重评估的正式实现
- 执行器 `executors.py` 仍是占位

结论：

- 主调度思路正确
- 但还处于“逻辑草稿”阶段，不是稳定执行框架

#### C. 模型层现状

当前模型层已经有 `BaseModel + ModelManager + 多提供方适配器` 的基本结构，这一点是好的。

但从代码看，当前模型层还存在这些典型工程问题：

- `BaseModel` 只定义了 `generate/stream_generate`，缺少 `health_check`、超时策略、异常统一封装
- `ModelManager` 是硬编码单模型选择，不是路由层
- `OpenAIModel`、`QianwenModel`、`DoubaoModel` 的请求封装不统一
- 模型名和 endpoint 在代码里写死，不利于环境切换
- 没有超时、重试、熔断、限流、调用统计
- `requirements.txt` 中未明确声明 `openai` 包
- 依赖里已有 `langchain-openai`，但当前实现又直接使用 `openai` SDK，策略不统一

结论：

- 当前模型层适合作为 provider demo
- 还不能视为正式的模型服务层

#### D. 工具层现状

工具层已经能体现“多功能助手”的方向，但目前大量工具是“功能演示级”实现：

- `DocumentParser`：仅支持 `txt/md/pdf`，没有元数据、编码检测、分页信息
- `SearchTool`：依赖网页抓取，稳定性差，且 `requirements.txt` 未声明 `beautifulsoup4`
- `Translator`：主逻辑依赖公开接口，异常时退回 mock 翻译，更适合 demo
- `TextProcessor`：摘要/关键词都还是非常轻量的启发式实现
- `TimeQuery`：可用，但能力单一
- `CodeExecutor`：直接落临时 Python 文件并执行，缺少沙箱隔离，是高风险实现
- `FileWriter`：允许写任意路径，缺少工作区限制与权限策略
- 当前工具全部返回字符串，缺少统一结果结构

结论：

- 工具层方向正确、分类合理
- 但当前实现多数仍偏原型，不适合直接拿去做开放式 Agent 调用

#### E. 记忆层现状

从代码看，短期和长期记忆都已经实现了最小闭环，但都还是原型级：

- `ShortTermMemory` 仅按消息条数截断
- 没有 token 控制、摘要压缩、会话隔离
- `LongTermMemory` 以 `documents + embeddings + pickle` 方式持久化
- 默认向量存储路径是 `data/vector_store`，既像目录又当文件用，命名不清晰
- 向量召回只返回原始文本，不返回来源、chunk、分数
- fallback embedding 是 hash 向量，只能作为占位，不能视作有效语义检索

结论：

- 现在的记忆层适合流程打通
- 不足以支撑可验证的知识检索质量

#### F. RAG 层现状

`RAGSystem` 当前本质上是一个薄封装：

- 调长期记忆检索
- 拼上下文
- 调模型生成回答

缺少：

- chunk 管道
- 文档导入/更新机制
- 元数据
- 引用
- 检索评估
- 召回与生成分离

结论：

- 目前还不是完整 RAG，只是“基于记忆检索的问答包装器”

#### G. 依赖与工程基础现状

基于代码与依赖文件比对，当前还有明显不一致项：

- `jieba` 已在 `complexity_judge.py`、`text_processor.py` 中使用，但 `requirements.txt` 未声明
- `beautifulsoup4` 已在 `search_tool.py` 中使用，但 `requirements.txt` 未声明
- `openai` SDK 被直接使用，但 `requirements.txt` 未声明
- `langchain_community` 的 embedding 被使用，但 `requirements.txt` 未声明对应包

此外，源码中存在较多中文注释/字符串显示异常，至少说明当前编码、终端显示或文件一致性需要尽快核查。

结论：

- 目前项目的“能否运行”受本地环境偶然性影响较大
- 环境规范必须作为 P0 处理项写入工程方案

---

## 3. 当前实现的主要问题与必须修正项

这一部分很关键。当前项目已经不是“没有想法”，而是“设计方向有了，但还没收敛成可维护、可运行、可交付的工程实现”。

### 3.1 入口与模块导入不一致

当前 `main.py` 中导入的是：

- `from agent.complexity_analyzer import ComplexityAnalyzer`

但实际仓库里更接近这个职责的文件是：

- `src/agent/complexity_analyzer_cankao.py`
- `src/agent/complexity_judge.py`

风险：

- 程序无法稳定启动
- 实际运行逻辑与设计意图不一致
- 后续协作开发时容易反复误判依赖关系

改进要求：

- 统一复杂度分析模块命名，只保留一套正式实现
- 建议正式文件名统一为 `src/agent/complexity_analyzer.py`
- 参考实现、实验实现、废弃实现迁移到 `experiments/` 或 `docs/archive/`

### 3.2 Agent 模块依赖关系混乱

`react_agent.py` 当前存在以下问题：

- 同时依赖 `complexity_judge` 和 `complexity_analyzer_cankao`
- 存在未完成方法与 TODO 较多
- 打印调试信息直接散落在业务流程中
- 规划结果、步骤解析、工具执行、结果汇总边界不清晰

风险：

- 主流程难以扩展
- 难以做单元测试
- 一旦工具数量增加，调度逻辑会快速失控

改进要求：

- 将 Agent 拆成“编排层 + 执行层 + 策略层”
- `ReactAgent` 只负责会话编排
- 任务规划、动作决策、工具调用、结果总结拆为独立服务

### 3.3 当前复杂度分析存在双轨实现

现状：

- 一套是规则/实体/多意图方向
- 一套是七维评分方向

问题不在于“有两套想法”，而在于“没有收敛成统一标准”。

改进建议：

- 保留“七维评分 + 高风险规则兜底”的主方案
- 将多意图识别、实体提取作为复杂度分析的输入特征，而不是平行系统
- 输出统一结构，例如：

```python
TaskAnalysisResult = {
    "intent": [...],
    "entities": [...],
    "parameters": {...},
    "complexity_score": 0.0,
    "complexity_level": "simple|medium|complex|high_risk",
    "execution_mode": "direct|plan_and_execute",
    "risk_flags": [...],
}
```

结合你补充的 [复杂度判定.md](C:\Users\HJW-236\Desktop\project\agentProject\src\agent\复杂度判定.md) 与 [意图提取流程.txt](C:\Users\HJW-236\Desktop\project\agentProject\src\agent\意图提取流程.txt)，可以进一步确认当前这部分的正确方向应该是：

- 意图提取先于复杂度评分，但二者必须共用同一个结构化中间结果
- 启发式规则优先级高于分类器，高于七维评分
- 单意图与多意图必须拆成两条流水线，而不是混在同一个分类出口里
- 分类器当前可以不训练完成，但接口、输入输出、置信度字段、降级逻辑必须先定死
- 大模型在这里应该是兜底器，不应该成为默认第一判断器

这意味着当前工程上最需要避免的，不是“规则写得不够多”，而是：

- 没有统一的 `IntentAnalysisResult`
- 没有把 V1/V2/V3 的设计演进收敛成正式主流程
- 没有把“规则命中、分类器判断、LLM 兜底、用户澄清”定义成可观测的状态机
- 没有把阶段一、阶段二已完成的逻辑和后续待训练分类器接口隔离开

建议把现有设计正式收敛为四阶段意图复杂度流水线：

1. 预处理层

- 文本清洗
- 分词
- 显式连接词识别
- 基础实体预提取

2. 意图识别层

- 多意图前筛
- 规则匹配
- 轻量分类器
- 不确定性检测
- LLM 结构化兜底
- UNKNOWN/澄清分支

3. 复杂度评估层

- 模糊任务规则
- 高风险规则
- 简单任务直通规则
- 七维评分
- 复杂度等级映射
- 执行模式映射

4. 调度决策层

- Micro
- Meso
- Meso Advanced
- Macro

这一版建议直接作为正式工程基线，而不是再保留多份“参考实现”并行存在。

### 3.3.1 意图复杂度模块的专项问题

结合 `src/agent/` 下现有代码，当前这块还存在以下专项缺口：

- `classifier.py` 还是占位，说明分类器能力尚未接入，但调度层尚未做好无分类器时的显式降级设计
- `uncertainty_detector.py` 还是占位，不确定性检测虽然在设计稿里很关键，但代码里还没有正式落地
- `executors.py` 还是占位，导致复杂度等级虽然有设计，但执行器边界还没有工程化
- `react_agent.py` 里已经有“简单/复杂任务分流”的主干，但还没和你在设计稿里定义的 `Micro / Meso / Macro` 对齐
- `complexity_judge.py` 与 `complexity_analyzer_cankao.py` 都在承载部分设计，但还没有一个正式统一出口

结论是：这部分已经完成“第一阶段预处理 + 第二阶段基础规则/判断”的雏形，但距离工程化还差一个标准化收口步骤。

### 3.3.2 建议的正式收口方式

建议在 `src/agent/` 或后续重构目录中只保留以下正式角色：

- `intent_pipeline.py`
- `intent_rules.py`
- `intent_classifier.py`
- `uncertainty_detector.py`
- `complexity_analyzer.py`
- `execution_router.py`

其中职责明确如下：

- `intent_pipeline.py`：串联单意图/多意图识别主流程
- `intent_rules.py`：规则库与规则匹配
- `intent_classifier.py`：轻量分类器统一接口，当前可先用 mock/stub
- `uncertainty_detector.py`：基于概率分布、熵、top1-top2 margin 做判断
- `complexity_analyzer.py`：七维评分 + 启发式规则 + 等级映射
- `execution_router.py`：将分析结果映射到 `Micro/Meso/Macro`

### 3.4 工具层缺少统一协议

当前工具虽然接入较多，但协议层仍不统一：

- 参数格式没有统一 schema
- 错误处理不一致
- 工具能力描述散落在代码里
- 缺少权限边界和安全策略

风险：

- Agent 生成调用参数时容易失败
- 后续增加工具会出现越来越多兼容成本
- 涉及代码执行、文件写入、网络搜索时存在安全风险

改进要求：

- 所有工具实现统一 `BaseTool` 接口
- 每个工具必须定义：名称、描述、参数 schema、返回 schema、异常类型、超时策略
- 引入工具注册机制，不再靠硬编码字典堆叠

### 3.5 记忆层仍是原型实现

当前问题：

- 短期记忆仅按条数裁剪，缺少 token/摘要机制
- 长期记忆把向量、文档直接以 pickle 保存，工程风险较高
- 向量化模型依赖不稳定，fallback 逻辑过于简单
- 缺少文档元数据与分块信息

改进要求：

- 短期记忆支持消息摘要与窗口裁剪
- 长期记忆引入标准文档结构：`id/content/source/chunk/meta/vector`
- 将向量库与原始文档分离存储
- RAG 检索增加 chunking、top-k、重排、来源引用

### 3.6 RAG 还未达到可用工程级别

当前 `RAGSystem` 更像简单包装器，缺少：

- 文档切分
- 文档去重
- 元数据追踪
- 引用片段返回
- 检索质量评估
- 索引更新策略

改进要求：

- 新增知识库导入管道
- 统一文档 chunk 格式
- 回答时返回证据片段与来源
- 区分“对话记忆检索”和“知识库检索”

### 3.7 测试体系缺失且现有测试与实现接口不完全匹配

当前 `src/agent/test_agent.py` 更接近脚本化验证，不是标准自动化测试；同时依赖的方法和实现未完全对齐。

改进要求：

- 切换到 `pytest`
- 为复杂度分析、工具层、记忆层、Agent 编排层分别编写单元测试
- 为典型多步任务增加集成测试
- 为高风险工具增加安全回归测试

### 3.8 工程基础设施不足

当前缺少：

- 配置分层
- 日志规范
- 错误码规范
- 数据目录规范
- API 层
- 部署脚本
- CI/CD
- 监控与可观测性

这部分是后续真正能不能“落地”的关键。

### 3.9 已有代码的直接整改清单

这一节不是未来愿景，而是基于当前代码应尽快处理的直接整改项。

#### 3.9.1 入口与包结构

必须修改：

- 去掉 `main.py` 中通过 `sys.path.append` 动态注入模块路径的方式
- 将 `src/` 正式包化，统一导入路径
- 明确 `main.py` 只作为启动入口，不承载装配细节
- 增加启动期依赖检查与配置检查

建议结果：

- `main.py` 负责启动
- `bootstrap.py` 负责依赖装配
- `config.py` 负责读取配置

#### 3.9.2 Agent 编排层

必须修改：

- 将 `ReactAgent` 拆分为 Orchestrator、Planner、Executor、Responder
- 计划输出从自然语言文本改为结构化对象
- 步骤执行结果改为结构化对象
- 加入失败重试、最大步数、状态跟踪
- 接入你设计稿中的 `Micro / Meso / Macro` 映射关系

#### 3.9.3 意图复杂度模块

必须修改：

- 合并 `complexity_judge.py` 与 `complexity_analyzer_cankao.py` 的正式职责
- 把多意图前筛、规则匹配、分类器占位、不确定性检测、LLM兜底串成单一流水线
- 定义统一 `AnalysisResult`
- 让分类器未训练完成时也能稳定降级运行
- 把 `UNKNOWN` 和用户澄清分支做成正式输出状态

#### 3.9.4 模型层

必须修改：

- 把 provider 配置从代码常量迁移到配置文件/环境变量
- 增加统一异常封装
- 增加 timeout/retry
- 增加 `health_check()`
- 区分 chat model 与 embedding model

建议修改：

- 模型调用统一返回标准对象，而不是只返回文本
- 记录 provider、model、耗时、token、失败原因

#### 3.9.5 工具层

必须修改：

- 所有工具统一返回结构化结果
- `CodeExecutor` 增加严格安全限制，未完成前默认关闭
- `FileWriter` 限制到工作区白名单目录
- `SearchTool` 替换掉脆弱的页面抓取方案，至少抽象成 provider 接口
- `Translator` 区分 mock 模式与真实模式，不能静默混用

建议修改：

- 每个工具补参数 schema
- 每个工具补风险等级
- 每个工具补超时控制

#### 3.9.6 记忆与 RAG

必须修改：

- 明确向量存储文件路径，不再使用含糊命名
- 长期记忆返回结果时附带 metadata
- 引入 chunk 文档结构
- 将 `RAGSystem` 升级为独立 pipeline，而不是简单 prompt 拼接

建议修改：

- 区分会话记忆与知识库记忆
- 为知识库导入增加去重和增量更新

#### 3.9.7 依赖管理

必须修改：

- 补齐 `requirements.txt` 中缺失依赖
- 增加 `.env.example`
- 固定关键依赖版本
- 增加开发依赖与运行依赖分层

建议最少补齐以下依赖：

- `openai`
- `jieba`
- `beautifulsoup4`
- `langchain-community`
- `pytest`

#### 3.9.8 源码质量

必须修改：

- 核查源码文件编码一致性
- 修复显示异常的中文字符串和注释
- 清理无实现占位文件与实验文件的正式入口引用

建议修改：

- 增加 `ruff` / `black` / `mypy`
- 增加 pre-commit

---

## 4. 工程化目标定义

### 4.1 产品目标

将当前多功能助手 Agent 升级为一个可稳定运行、可扩展、可测试、可部署的任务型智能助手系统，支持：

- 单轮问答
- 多轮对话
- 多工具调用
- 多步任务规划与执行
- 文档理解与知识库问答
- 风险任务识别与安全约束
- 后续 API 化和前端接入

### 4.2 工程目标

目标不是只“能跑通 demo”，而是达到以下标准：

- 本地开发环境可一键启动
- 模块边界清晰
- 配置与密钥管理规范
- 支持单元测试和集成测试
- 支持日志追踪与问题排查
- 支持 API 服务化部署
- 支持后续新增模型、工具、知识库而不大改主流程

---

## 5. 目标架构设计

### 5.1 推荐总体架构

建议采用以下分层：

1. 接入层

- CLI
- HTTP API
- 未来可接 Web UI / 企业微信 / 飞书 / 钉钉

2. Agent 编排层

- 会话管理
- 上下文装配
- 复杂度分析
- 执行模式选择
- 任务规划
- 多步执行
- 最终回答生成

3. 能力服务层

- 模型服务
- 工具服务
- 记忆服务
- RAG 服务
- 安全审计服务

4. 基础设施层

- 配置管理
- 日志
- 数据存储
- 向量索引
- 测试框架
- 部署脚本

### 5.2 推荐目录结构

建议重构为：

```text
agentProject/
  app/
    api/
      routers/
      schemas/
    agent/
      orchestrator.py
      planner.py
      executor.py
      analyzer.py
      responder.py
    models/
      base.py
      manager.py
      providers/
    memory/
      short_term.py
      long_term.py
      schemas.py
    rag/
      pipeline.py
      retriever.py
      chunker.py
      ranker.py
    tools/
      base.py
      registry.py
      implementations/
    safety/
      policy.py
      guardrails.py
    core/
      config.py
      logger.py
      exceptions.py
      constants.py
    storage/
      vector_store/
      knowledge_base/
      sessions/
  tests/
    unit/
    integration/
    regression/
  scripts/
  docs/
  main.py
  requirements.txt
  .env.example
  README.md
```

说明：

- 如果你暂时不想大改目录，可以先在 `src/` 下按这个结构逐步迁移
- 不建议继续把“实验稿、参考稿、正式稿”混在同一个模块目录中

---

## 6. 核心模块详细设计

### 6.1 Agent 编排层

推荐拆成 5 个核心职责：

#### 6.1.1 Analyzer

职责：

- 文本预处理
- 意图识别
- 多意图识别
- 参数提取
- 风险识别
- 复杂度评分
- 执行模式判定

输出统一数据结构：

```python
class AnalysisResult:
    query: str
    tokens: list[str]
    intents: list[str]
    entities: list[str]
    parameters: dict
    risk_flags: list[str]
    ambiguity_flags: list[str]
    intent_source: str
    classifier_confidence: float | None
    uncertainty_score: float | None
    complexity_score: float
    complexity_level: str
    execution_mode: str
```

这里建议把你现在的“意图提取设计”和“复杂度判定设计”合并成 Analyzer 的内部子流水线，而不是拆成两个平级黑盒。推荐的 Analyzer 内部顺序如下：

1. `preprocess`

- 清洗文本
- 分词
- 标准化同义词
- 标记显式连接词

2. `detect_intent`

- 多意图前筛
- 规则命中
- 轻量分类器预测
- 不确定性判断
- LLM 兜底
- UNKNOWN 或澄清分支

3. `extract_entities_and_parameters`

- 时间、地点、数量、文件、格式、对象、工具名
- 参数完整性检查
- 必填参数缺失标记

4. `apply_heuristics`

- 模糊优先
- 高风险优先
- 简单任务直通

5. `score_complexity`

- 七维评分
- 权重汇总
- 风险加权

6. `route_execution`

- 映射到 `micro/meso/meso_advanced/macro`

#### 6.1.1.1 意图识别专项设计

这部分建议正式写成“可替换、可降级、可训练”的模块，而不是直接依赖某一种实现。

推荐接口：

```python
class IntentClassifier:
    def predict_single(self, text: str, tokens: list[str]) -> dict:
        ...

    def predict_multi(self, text: str, tokens: list[str]) -> dict:
        ...
```

当前阶段约定：

- 分类器未训练完成时，返回 `not_ready=True`
- 调度层收到 `not_ready=True` 时，自动走 `规则 -> LLM兜底`
- 分类器训练完成后，只替换实现，不改 Analyzer 主流程

推荐的意图识别输出：

```python
{
    "intents": ["search", "summarize"],
    "source": "rule|classifier|llm|fallback",
    "confidence": 0.91,
    "is_multi_intent": True,
    "unknown": False,
    "needs_clarification": False,
}
```

#### 6.1.1.2 单意图 / 多意图双通道

结合你现有设计稿，建议显式采用双通道而不是统一分类：

- 单意图通道：规则匹配 -> 单标签分类器 -> 多意图复核 -> LLM 兜底
- 多意图通道：显式连接词前筛 -> 多意图规则匹配 -> 多标签分类器 -> LLM 兜底

这样做的价值是：

- 降低单标签分类器误把并列任务压成单意图的概率
- 保留你设计里对“然后、同时、以及、并”等连接词的结构性利用
- 让后续训练数据也能按单意图/多意图拆分管理

#### 6.1.1.3 不确定性检测

你设计稿里这部分非常关键，建议升格为正式模块，而不是零散条件判断。

建议指标：

- `top1_prob`
- `top2_prob`
- `margin = top1 - top2`
- `entropy`
- `is_uniform_distribution`

建议阈值先配置化，不写死在代码里：

```python
INTENT_THRESHOLD_HIGH = 0.85
INTENT_THRESHOLD_LOW = 0.60
INTENT_MARGIN_THRESHOLD = 0.20
INTENT_ENTROPY_THRESHOLD = 0.70
MULTI_INTENT_THRESHOLD = 0.50
```

建议策略：

- 高置信度：直接采用
- 中置信度但区分明显：采用并标记 `reviewable`
- 低置信度或高熵：降级到 LLM
- LLM 仍无法确定：输出 `UNKNOWN` 并进入澄清

#### 6.1.1.4 LLM 兜底边界

你在设计里已经提到一个很重要的点：即使用大模型，也不能让它自由发挥。这个原则建议正式写入工程方案。

LLM 兜底必须满足：

- 只能从预定义意图集合中选择
- 允许输出 `UNKNOWN`
- 最多输出 N 个意图
- 必须返回结构化 JSON
- 失败时不能 silently fallback 成某个默认意图

推荐限制：

- 单意图最多返回 1 个
- 多意图最多返回 3 个
- 返回结果必须附带 `reason` 和 `confidence_band`

#### 6.1.1.5 参数完整性与澄清触发

复杂度模块不应只判断“这是什么意图”，还应判断“这个意图是否已经可执行”。

建议为每类意图维护参数规范：

```python
INTENT_PARAM_SPEC = {
    "search": ["topic"],
    "translate": ["content", "target_language"],
    "summarize": ["content_or_file"],
    "write": ["topic", "format"],
}
```

若意图识别成功但缺少关键参数：

- 不要立刻执行
- 不要直接判失败
- 转入 `macro clarification` 分支

这一步能显著减少错误执行和无效工具调用。

#### 6.1.1.6 与执行器的映射关系

建议在 Analyzer 输出中直接包含调度建议，供 Orchestrator 无状态消费：

```python
{
    "complexity_level": "simple",
    "execution_mode": "micro",
    "requires_clarification": False,
    "can_direct_execute": True,
}
```

映射关系建议固定如下：

- `ambiguous` -> `macro`
- `simple` -> `micro`
- `medium` -> `meso`
- `complex` -> `meso_advanced`
- `high_risk` -> `meso_advanced` + `cautious_mode`

#### 6.1.1.7 当前代码与设计稿的对应关系

目前大致可以这样理解：

- `ComplexIntent_filter.py`：多意图前筛 / 规则命中雏形
- `entity_extractor.py`：实体与参数提取雏形
- `heuristic_rules.py`：启发式规则层雏形
- `complexity_judge.py`：结构化分析 + 七维评分方向
- `complexity_analyzer_cankao.py`：另一套评分与映射参考实现
- `classifier.py`：未来轻量分类器接口占位
- `uncertainty_detector.py`：未来不确定性检测器占位

所以后续不是从头重做，而是把这些零散能力收敛成统一流水线。

#### 6.1.2 Planner

职责：

- 针对复杂任务生成步骤计划
- 为每一步绑定候选工具
- 输出结构化计划，而不是纯文本步骤

建议输出：

```python
class TaskPlan:
    goal: str
    steps: list[PlanStep]

class PlanStep:
    id: str
    description: str
    tool_name: str | None
    args: dict
    expected_output: str
```

#### 6.1.3 Executor

职责：

- 执行计划步骤
- 记录中间结果
- 处理工具异常
- 支持重试和降级

#### 6.1.4 Context Builder

职责：

- 汇总短期记忆
- 检索长期记忆/RAG
- 拼接工具中间结果
- 控制上下文长度

#### 6.1.5 Responder

职责：

- 生成最终对用户回复
- 对多步结果进行总结
- 在必要时补充风险提示、来源引用、失败说明

### 6.2 模型层

目标：支持多模型、统一接口、可切换、可扩展。

建议能力：

- `generate()`
- `stream_generate()`
- `embedding()`
- `health_check()`
- `get_model_info()`

建议新增：

- 模型配置中心
- 按任务类型路由模型
- 超时与重试配置
- 成本统计与调用日志

推荐策略：

- 简单分类/格式化任务走低成本模型
- 规划、总结、多轮复杂问答走高质量模型
- 嵌入模型独立配置，不与主对话模型混用

### 6.3 工具层

建议定义统一基类：

```python
class BaseTool:
    name: str
    description: str
    parameters_schema: dict
    timeout: int = 10

    def run(self, **kwargs) -> dict:
        raise NotImplementedError
```

返回值统一：

```python
{
    "success": True,
    "data": {...},
    "message": "...",
    "error": None,
}
```

工具按风险级别分类：

- 低风险：时间查询、数学计算、文本处理
- 中风险：文档解析、翻译、搜索
- 高风险：代码执行、文件写入、未来的系统命令执行

工程要求：

- 每个工具必须声明输入参数
- 每个工具必须做参数校验
- 高风险工具必须经过策略审查
- 文件类工具必须限制工作目录
- 代码执行工具必须做沙箱隔离或严格禁用系统级能力

### 6.4 记忆层

#### 短期记忆

建议支持：

- 最近 N 轮消息
- 自动摘要压缩
- 角色区分
- 会话级唯一 ID

#### 长期记忆

建议拆分两类：

- 对话长期记忆：用户偏好、历史任务摘要
- 知识长期记忆：文档、FAQ、业务知识库

当前阶段优先做知识记忆，不建议一开始把“用户画像长期记忆”做得太重。

### 6.5 RAG 层

建议拆成标准流程：

1. 文档导入
2. 文本切块
3. 元数据抽取
4. 向量化
5. 存储索引
6. 查询检索
7. 重排
8. 拼接上下文
9. 生成带引用的回答

最低可用标准：

- 支持 `txt/md/pdf`
- 文档切块 configurable
- 支持来源显示
- 支持按知识库/文档过滤检索
- 支持增量导入

### 6.6 安全层

这是多功能 Agent 落地时必须提前建设的部分。

至少需要覆盖：

- 高风险领域识别：法律、医疗、金融、隐私、账号密码
- 工具权限控制
- 文件写入白名单目录
- 代码执行隔离
- Prompt Injection 基础防护
- 用户输入与工具输出的敏感信息脱敏

---

## 7. 分阶段开发路线图

这里按照“能交付、能验证、能逐步迭代”的方式拆分。

### 阶段 0：现状收敛与项目纠偏

目标：把当前原型整理成可继续开发的基线版本。

任务：

- 统一复杂度分析模块，移除重复实现
- 统一意图识别主流程，确定单意图/多意图双通道
- 修正 `main.py` 与实际模块导入不一致问题
- 修正 `requirements.txt` 与真实代码依赖不一致问题
- 修正 provider 配置硬编码问题
- 修正高风险工具默认可用的问题
- 清理无法运行或废弃的实验文件命名
- 补充 `README.md`
- 补充 `.env.example`
- 补充基础日志模块
- 梳理数据目录：`data/`, `logs/`, `storage/`

交付物：

- 可启动的 CLI 版本
- 明确的模块依赖结构
- 开发环境说明

### 阶段 1：单 Agent 可用版本

目标：先把单 Agent 主流程真正跑稳。

任务：

- 实现统一 `Analyzer`
- 落地意图复杂度统一接口与中间结果结构
- 接入规则层、分类器占位层、LLM 兜底层
- 实现 `UNKNOWN / 澄清 / 高风险 / 简单直通` 四类分支
- 实现统一 `ToolRegistry`
- 实现简单任务直出流程
- 实现复杂任务的结构化计划输出
- 将 `ReactAgent` 收敛为真正的编排层
- 为工具、模型、RAG、记忆统一标准返回结构
- 工具调用结果统一封装
- 增加异常处理和兜底回答

验收标准：

- 能稳定处理 20+ 条典型指令
- 简单任务成功率达到可演示水平
- 复杂任务能输出计划并至少执行 2 到 3 步

### 阶段 2：RAG 与记忆增强

目标：让 Agent 具备可靠的文档问答和知识检索能力。

任务：

- 建立文档导入管道
- 引入 chunking 和 metadata
- 优化检索与回答拼接
- 输出引用来源
- 支持会话上下文与知识库上下文联合装配
- 替换长期记忆中的占位 embedding fallback 设计

验收标准：

- 指定文档问答可稳定返回相关答案
- 返回结果附带来源
- 检索失败时能正确提示

### 阶段 3：服务化 API

目标：从脚本程序升级为服务。

建议框架：`FastAPI`

任务：

- 增加 `/chat` 接口
- 增加 `/knowledge/upload` 接口
- 增加 `/tools/list` 接口
- 增加 `/health` 接口
- 支持会话 ID
- 增加请求日志与错误处理中间件

验收标准：

- 本地可通过 HTTP 调用
- 可被前端或第三方系统接入
- 核心接口具备基本稳定性

### 阶段 4：可观测性与工程稳定性

任务：

- 增加结构化日志
- 增加 trace_id / session_id
- 增加性能指标统计
- 增加工具耗时记录
- 增加失败重试和熔断策略

验收标准：

- 可以定位一次请求经过了哪些模块
- 可以识别慢调用和失败点

### 阶段 5：部署与交付

任务：

- Docker 化
- 生产环境配置区分
- 编写启动脚本
- 编写部署文档
- 接入 CI

验收标准：

- 新环境可快速部署
- 提交代码后可自动跑测试

---

## 8. 建议的近期开发优先级

如果你希望尽快做出一个“可演示、可继续迭代”的版本，建议按下面顺序推进。

### P0：必须先做

- 统一模块命名与导入
- 把意图识别、复杂度评分、执行路由收敛到同一 Analyzer 出口
- 让主流程可稳定运行
- 抽象工具统一协议
- 建立日志与配置管理
- 建立标准测试框架

### P1：优先做

- 结构化复杂任务规划
- RAG 文档切块与引用
- 工具权限与错误处理
- API 服务化

### P2：后续增强

- 模型路由策略
- 记忆摘要压缩
- 会话持久化
- 前端 UI
- 多 Agent 协同

---

## 9. 技术选型建议

### 9.1 后端

- Python 3.11 或 3.12
- FastAPI
- Pydantic
- Uvicorn

### 9.2 测试

- pytest
- pytest-mock
- httpx（API 测试）

### 9.3 向量与检索

- 初期：FAISS
- 中期：FAISS + 标准文档元数据管理
- 后期可按需要迁移 Milvus / pgvector / Elasticsearch

### 9.4 配置与日志

- `python-dotenv`
- `pydantic-settings`
- `logging` 或 `loguru`

### 9.5 代码质量

- `ruff`
- `black`
- `mypy`

---

## 10. 配置与环境规范

建议新增 `.env.example`，至少包含：

```env
MODEL_NAME=openai
OPENAI_API_KEY=
OPENAI_BASE_URL=
QIANWEN_API_KEY=
DOUBAO_API_KEY=
EMBEDDING_PROVIDER=
EMBEDDING_API_KEY=
BING_API_KEY=
VECTOR_STORE_PATH=./storage/vector_store/index.bin
LOG_LEVEL=INFO
APP_ENV=dev
```

配置规范：

- 业务代码中不直接散落 `os.getenv()`
- 统一由 `config.py` 管理配置加载
- 区分 `dev/test/prod`

---

## 11. 测试方案

### 11.1 单元测试

覆盖：

- 单意图规则命中
- 多意图前筛与多标签输出
- 分类器未就绪时的降级逻辑
- 不确定性检测阈值判断
- UNKNOWN 与用户澄清触发
- 复杂度分析结果
- 工具入参与返回
- 记忆增删查
- RAG 检索结果
- 模型管理路由逻辑

### 11.2 集成测试

覆盖：

- 用户输入 -> 意图识别 -> 复杂度判断 -> 执行模式路由
- 用户输入 -> 分析 -> 规划 -> 工具执行 -> 回答生成
- 文档导入 -> 检索 -> 回答引用

### 11.3 回归测试

建议建立典型样本集：

- 简单问答
- 单意图高置信度
- 单意图低置信度降级到 LLM
- 显式多意图
- 隐式多意图误判复核
- UNKNOWN 澄清场景
- 文档总结
- 文件解析
- 多步任务
- 高风险任务拦截
- 工具执行失败回退

### 11.4 验收样例集

建议维护 `tests/fixtures/cases.yaml`，把真实业务指令沉淀为测试资产。

---

## 12. 部署方案

### 12.1 本地开发部署

- 虚拟环境
- `.env`
- `pip install -r requirements.txt`
- `python main.py`

### 12.2 服务部署

建议新增：

- `app/api/server.py`
- `Dockerfile`
- `docker-compose.yml`

启动方式建议：

```bash
uvicorn app.api.server:app --host 0.0.0.0 --port 8000
```

### 12.3 生产部署建议

- 统一日志落盘或接日志平台
- 密钥走环境变量或密钥管理服务
- 向量索引与业务数据分目录管理
- 关键接口加限流与超时

---

## 13. 风险与应对

### 风险 1：模块继续并行试验，主干长期不收敛

应对：

- 设立正式主干模块
- 实验代码移出主路径

### 风险 2：工具越来越多，调用成功率反而下降

应对：

- 统一工具协议
- 工具描述结构化
- 增加强校验与工具选择策略

### 风险 2.1：意图复杂度模块长期停留在“半成品”

应对：

- 先把规则、分类器占位、LLM兜底、澄清分支统一成正式流水线
- 训练能力后置，但接口与阈值配置前置
- 用测试样本而不是人工感觉来调阈值

### 风险 3：RAG 做出来但效果不稳定

应对：

- 文档切块标准化
- 增加检索评估集
- 输出引用来源方便人工校验

### 风险 4：高风险能力带来安全问题

应对：

- 高风险任务识别
- 文件与代码工具强限制
- 关键动作必须显式授权

### 风险 5：项目能跑 demo，但无法持续维护

应对：

- 尽快补齐测试、日志、配置、目录规范
- 从“脚本思维”切换到“服务工程思维”

---

## 14. 建议的两周落地排期

### 第 1 周

1. 统一复杂度分析模块与命名
2. 修正主入口与依赖关系
3. 重构 ToolManager 为统一注册协议
4. 增加配置管理、日志模块、`.env.example`
5. 建立 pytest 基础设施

### 第 2 周

1. 实现结构化 Planner + Executor
2. 完成文档导入与基础 RAG 流程
3. 增加 FastAPI 接口
4. 增加核心集成测试
5. 输出第一版 API 文档与部署说明

两周目标不是做完所有能力，而是产出一个“可运行、可测试、可展示、可继续开发”的 V1 工程基线。

---

## 15. V1 验收标准

满足以下条件时，可认为 V1 达到工程化可落地水平：

- CLI 与 API 两种入口至少具备一种稳定可用
- 简单任务执行成功率较高
- 复杂任务可输出结构化计划并执行部分步骤
- 工具层具备统一协议
- RAG 支持基础文档问答与来源引用
- 具备标准日志
- 具备基础自动化测试
- 配置与环境变量管理清晰
- 项目目录与模块边界清晰

---

## 16. 对当前项目的最终建议

你现在这套多功能助手 Agent，最有价值的地方不是“已经实现了多少功能”，而是你已经把一个真正的任务型智能助手需要的关键拼图都想到了：模型、工具、记忆、RAG、复杂度分流、多步执行。

当前最需要做的不是继续叠功能，而是先“收敛主干、统一协议、补足工程基础设施”。只要把这一步做好，后面无论接 API、做前端、扩工具、加知识库、做多 Agent，都能顺着一个稳定底座往上长。

建议你接下来的开发原则就一句话：

**先把主干跑稳，再把能力做强。**
